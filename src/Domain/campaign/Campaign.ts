export type Campaign = {
  id: string;
  title: string;
  description: string;
  location: string;
  locationAddress: string;
  maxVolunteers: number;
  startDate: string;
  endDate: string;
  imageUrl: string;
};
